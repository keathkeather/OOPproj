monthlyDue formula:

    ALTER TABLE `loan` 
    MODIFY COLUMN `loanBalance` DOUBLE(10, 2) 
    GENERATED ALWAYS AS (ROUND(monthlyDue * loanDuration, 2)) 
    VIRTUAL;

monthlyDue formula:

    ALTER TABLE `loan`
    MODIFY COLUMN `loanBalance` DOUBLE(10, 2) 
    GENERATED ALWAYS AS (ROUND(monthlyDue * loanDuration, 2)) 
    VIRTUAL;

private Connection connection;

    private String dbUrl = "jdbc:mysql://localhost:3306/oop";
    private String username = "root";
    private String password = "";

// establish database connection
        try {
            connection = DriverManager.getConnection(dbUrl, username, password);
        } catch (SQLException ex) {
            ex.printStackTrace();
            JOptionPane.showMessageDialog(null, "Error connecting to the database.", "Database Error",
                    JOptionPane.ERROR_MESSAGE);
        }